{
  "name": "test-token-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "contract/*",
    "server/*"
  ],
  "scripts": {
    "install:all": "npm install --workspaces",
    "build": "npm run build --workspaces",
    "test": "npm test --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^18.0.0",
    "eslint": "^8.0.0",
    "prettier": "^2.8.0"
  }
} 